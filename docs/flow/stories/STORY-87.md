---
aliases:
  - STORY-87
type: task
status: Active
priority: P1
started: 2026-06-15
id: STORY-87
tags: [workflow]
domain: workflow_meta
domain_tags: []
---

# STORY-87: Workflow Layered Architecture — OSI-Style Enforcement Model

> **Meta-Project Integration Note:** This is **not** a core ADLAI feature. This story
> tracks the evolution and integration of the **Workflow Engine** (a sibling/meta-project)
> into the ADLAI codebase.
>
> - **Meta-Project (The Factory):** `~/X/TEAMFLOW/conductor` + `~/X/TEAMFLOW/RVC`
> - **Target Project (The Product):** `~/Documents/ADLAI`
> - **Integration Point:** `adlai-vault/00_Project/session_bootstrap.sh` & `WORKFLOW.md`

---

## The Core Problem

A flat markdown document (WORKFLOW.md) with 19 numbered steps across 4 phases.
The LLM reads it and can freely ignore any step. It's a **suggestion**, not a **protocol**.

Test proof: Big Pickle read the workflow, announced "following the ritual," then
immediately went off-piste to investigate OpenCode internals instead of
SYNC → announce → ENGAGE → ACT.

**Root cause:** There is nothing enforcing the sequence. The workflow has no teeth.

---

## Current Status Overview

| Phase | Prompt code | Auto-artifact | Validation gate | Tests | Status |
|-------|-------------|---------------|-----------------|-------|--------|
| `--phase sync` | ✅ Done ([[STORY-92]]) | ✅ Done ([[STORY-93]]) | ✅ HARD STOP boundary | ✅ 12 tests | 🟢 Built |
| `--phase engage` | ✅ Done | ✅ auto-generated by sync | ✅ gates on status=ready | ✅ 6 tests | 🟢 Built |
| `--phase act` | ⚠️ Written but **broken** | ❌ Not implemented | ⚠️ Written but **broken** | ❌ None | 🔴 Blocked |
| `--phase wrap` | ⚠️ Written but **broken** | ❌ Not implemented | ⚠️ Written but **broken** | ❌ None | 🔴 Blocked |

### Known bug: ACT/WRAP cannot find story file

`session_bootstrap.sh:126-128` only sets `$STORY_FILE` for `sync|engage`:

```bash
if [[ -n "$PHASE" && ("$PHASE" == "sync" || "$PHASE" == "engage") ]]; then
  STORY_FILE=$(find "$VAULT/10_Issues" -name "${ISSUE_ID}-*.md" -type f 2>/dev/null | head -1 || true)
fi
```

Both ACT (`:278`) and WRAP (`:348`) gate on `[[ -z "$STORY_FILE" ]]` which is **always true** for those phases. Result: `--phase act` and `--phase wrap` always exit 1 immediately.

**Fix:** Move story file discovery outside the phase conditional, or duplicate it for ACT/WRAP.

---

## The OSI-Style Model

Each layer constrains the layer below it. Protocols between layers are explicit.
A layer cannot be skipped — the lower layer never receives the data to skip it.

```
┌──────────────────────────────────────────────────┐
│ LAYER 5 — Bootstrap                               │
│   session_bootstrap.sh                            │
│   "Assemble everything the agent needs. Deliver   │
│    once."                                         │
│   Protocol: context payload → Layer 4             │
│   STATUS: ✅ BUILT (v2, safe heredoc, lean)       │
├──────────────────────────────────────────────────┤
│ LAYER 4 — Conductor (outer enforcement ring)      │
│   Plan → Approve → Act → QA → Commit              │
│   Hard LangGraph nodes, checkpointed, human       │
│   interrupt at Approve. Stateless workers.        │
│   Protocol: node_name + state → Layer 3           │
│   STATUS: ✅ BUILT (~/X/TEAMFLOW/conductor)                 │
├──────────────────────────────────────────────────┤
│ LAYER 3 — Phase Gating (inner enforcement ring)   │
│   SYNC → ENGAGE → ACT → WRAP                      │
│   Each phase = one prompt. One LLM invocation.    │
│   Phase N's output is Phase N+1's input. The LLM  │
│   cannot skip phases because it never sees them.  │
│   Protocol: phase + required_artifact → Layer 2   │
│   STATUS: 🟡 Partial (SYNC/ENGAGE built,          │
│           ACT/WRAP broken)                        │
├──────────────────────────────────────────────────┤
│ LAYER 2 — Artifacts (handoff protocol)            │
│   STATE.json, DECISIONS.md, GOTCHAS.md            │
│   "What happened, what was decided, what to       │
│    avoid." Cross-session state is always here.    │
│   Protocol: read state → act → write state        │
│   STATUS: ✅ BUILT (initialized, working)          │
├──────────────────────────────────────────────────┤
│ LAYER 1 — Tool Sandbox                            │
│   Permissions: read-only in SYNC, write in ACT,   │
│   no destructive ops ever.                        │
│   Protocol: phase → allowed_actions               │
│   STATUS: ⚠️ PARTIAL (Hermes has perms, not       │
│           phase-aware)                            │
├──────────────────────────────────────────────────┤
│ LAYER 0 — Model (substrate)                       │
│   The LLM itself. No control, only steering.      │
│   STATUS: ⚠️ PARTIAL (big-pickle works, local     │
│           Ollama broken for headless)             │
└──────────────────────────────────────────────────┘
```

---

## Interlayer Protocols

| From → To | What flows | How |
|---|---|---|
| L5 → L4 | Context payload | Bootstrap script output → conductor worker prompt |
| L4 → L3 | Pipeline node + state | Conductor feeds phase-specific prompt to agent |
| L3 → L2 | Phase completion artifact | SYNC produces announcement, ENGAGE updates state, ACT produces code, WRAP produces commit |
| L2 → L1 | Current phase | Phase name determines tool permissions |
| L1 → L0 | Available tool set | Model receives filtered tool list |

---

## The Missing Layer: L3 Phase Gating

**What it must do:**

A single issue run becomes 4 separate LLM invocations. Each invocation gets a
prompt that describes ONLY its phase. The prompt ends with a hard boundary:
"STOP here. Do not proceed."

```
INVOCATION 1 (SYNC):
  Prompt: "Read STATE.json, the issue, linked specs, decisions, gotchas.
           Announce what you found. Do NOT write code. Do NOT transition."
  → Output: SYNC artifact updated to status=ready

INVOCATION 2 (ENGAGE):
  Prompt: "Read the SYNC artifact. Transition the issue. Update STATE.json.
           Read relevant specs for the code you'll change. Do NOT write code yet."
  → Output: ENGAGE artifact with plan

INVOCATION 3 (ACT):
  Prompt: "Read the engage plan. Implement. Update specs if code changed.
           Run tests. Fix failures (max 3 attempts). Do NOT commit until QA passes."
  → Output: tests pass (exit code 0) + ACT artifact

INVOCATION 4 (WRAP):
  Prompt: "Commit. Transition issue to Review. Append to DECISIONS.md and
           GOTCHAS.md. Update STATE.json to clear. Announce result."
  → Output: commit SHA + clean STATE.json + WRAP artifact
```

---

## Implementation Status by Phase

### `--phase sync` ✅ DONE ([[STORY-92]] + [[STORY-93]])

- SYNC-only prompt with HARD STOP boundary
- Auto-generates pending artifact in story file (deterministic fields)
- Agent reviews and flips `status=pending` → `status=ready`
- 12 tests covering artifact target, forbidden rules, hard stop

### `--phase engage` ✅ DONE

- Validates story file exists
- Validates SYNC artifact exists with `status=ready`
- Outputs ENGAGE prompt (plan only, no implementation)
- 6 tests covering validation gates

### `--phase act` 🔴 BLOCKED — needs fix + tests

**Prompt implemented** at `session_bootstrap.sh:277-345`:
- Validates SYNC artifact exists + ready
- Validates ENGAGE artifact exists + ready
- Instructs agent to implement, run QA gate (max 3 attempts), write ACT artifact

**Bug:** `$STORY_FILE` not set for this phase — always fails at line 278.

**Missing:** ACT artifact auto-generation (template block).

**Missing:** Tests for:
- ACT prompt contains correct phase instructions
- ACT validates SYNC + ENGAGE artifacts exist
- ACT rejects missing/pending artifacts
- ACT prompt references QA gate correctly

### `--phase wrap` 🔴 BLOCKED — needs fix + tests

**Prompt implemented** at `session_bootstrap.sh:347-421`:
- Validates all prior artifacts (SYNC, ENGAGE, ACT) ready
- Instructs agent to commit, transition, log decisions/gotchas, clear state

**Bug:** `$STORY_FILE` not set for this phase — always fails at line 348.

**Missing:** WRAP artifact auto-generation (template block).

**Missing:** Tests for:
- WRAP prompt contains correct phase instructions
- WRAP validates all 3 prior artifacts
- WRAP rejects incomplete prior phases
- WRAP prompt references commit + rvc transition

---

## Bottlenecks Mapped to Layers

| Bottleneck | Layer | Status |
|---|---|---|
| Context bloat (74KB+) | L5 | ✅ Fixed — lean prompt, WORKFLOW.md not dumped |
| Bash heredoc unsafe | L5 | ✅ Fixed — temp file + safe heredoc |
| **Model ignores workflow** | **L3** | 🟡 Partial — SYNC/ENGAGE enforced, ACT/WRAP broken |
| **$STORY_FILE not set for ACT/WRAP** | **L3** | 🔴 Bug — ACT/WRAP always exit 1 |
| ACT/WRAP artifact auto-generation | L3 | ❌ Missing |
| ACT/WRAP test coverage | L3 | ❌ Missing |
| Ollama provider broken | L0 | ⚠️ Workaround: big-pickle |
| Shallow git context | L5 | ⚠️ Low priority |

---

## Action Plan

### Phase 1: Fix ACT/WRAP + tests (immediate)

1. **Fix `$STORY_FILE` bug** — move story file discovery outside the `sync|engage` conditional so ACT/WRAP can find it
2. **Verify ACT/WRAP prompts work** — run `--phase act` and `--phase wrap` with valid artifacts, confirm they produce correct prompts
3. **Add ACT/WRAP tests** — 6+ tests covering prompt content, validation gates, and artifact detection
4. **Add ACT artifact auto-generation** (optional) — mirror SYNC pattern: auto-generate ACT artifact template in story file

### Phase 2: Manual 4-phase test run

5. **Run all 4 phases sequentially** on a test issue (e.g., STORY-87 itself)
6. **Verify artifact gates** — each phase's output feeds correctly into the next
7. **Verify agent compliance** — agent follows the flow without off-piste navigation (2 test runs)

### Phase 3: Conductor integration (optional)

8. **Wire `--phase` flow into `~/X/TEAMFLOW/conductor` ActNode as sub-graph**

---

## RVC Dependency & Pre-flight Check

### Problem

`session_bootstrap.sh:62-65` calls `rvc issue` and `rvc context`
unconditionally. If `rvc` is not in PATH, the commands silently fail via
`|| echo` fallback, producing `"(issue not found)"` and `"(no linked specs)"`
in the prompt. The user gets no indication that the prompt is degraded.

This is a **soft dependency** — the script works without rvc, but the
prompt quality is lower (missing issue title, description, linked specs).

### Solution: Pre-flight health check ✅ DONE

A non-fatal pre-flight check was added at line 39:

```bash
if ! command -v rvc &>/dev/null; then
  echo "WARN: rvc CLI not found in PATH — issue content and linked specs will be placeholder text." >&2
fi
```

This warns on stderr without changing behavior. The existing `rvc issue` /
`rvc context` calls (with `|| echo` fallback) remain unchanged — no
degradation.

### Test coverage (4 tests)

| Test | What it verifies |
|------|-----------------|
| `test_sync_fallback_text_without_rvc` | Without rvc, issue content shows `"(issue not found)"` fallback |
| `test_sync_structure_preserved_without_rvc` | Prompt structure intact without rvc |
| `test_legacy_fallback_text_without_rvc` | Legacy mode also uses fallback text without rvc |
| `test_warning_emitted_when_rvc_missing` | Stderr warning mentions rvc when PATH excludes it |

Tests run in `TestRvcAbsentBehavior` in `test_session_bootstrap.py`.

---

## Definition of Done

- [x] `session_bootstrap.sh --phase sync STORY-NN` outputs SYNC-only prompt with HARD STOP boundary, read-only
- [x] `--phase engage` works — validates SYNC artifact exists with `status=ready`
- [ ] `--phase act` works — validates SYNC+ENGAGE ready, outputs ACT prompt with QA gate instructions
- [x] Bug fixed: `$STORY_FILE` accessible from ACT/WRAP phases
- [ ] `--phase wrap` works — validates all prior phases ready, outputs WRAP prompt with commit/transition instructions
- [ ] ACT and WRAP phases have auto-generated artifact templates (optional, nice-to-have)
- [ ] Manual 4-phase run on STORY-87 completes with proper artifacts at each gate
- [ ] Each phase's output feeds correctly into the next phase's context
- [x] RVC pre-flight health check implemented (4 tests)
- [ ] Full test coverage for ACT and WRAP phases (6+ tests each)
- [ ] Agent follows the flow (no off-piste navigation) in at least 2 test runs
- [ ] QA gate: `uv run pytest backend/tests/unit -q` exits 0
- [ ] (Optional) Conductor ActNode sub-graph wired to `--phase` flag
